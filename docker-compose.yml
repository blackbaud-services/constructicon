build:
  image: arhea/yarn:6
  working_dir: /code
  volumes:
    - .:/code
    - $SSH_AUTH_SOCK:/ssh-agent
  environment:
    - AWS_KEY
    - AWS_SECRET
    - NPM_USER
    - NPM_PASS
    - NPM_EMAIL
    - SLACK_WEBHOOK_URL
    - BUILD_URL
    - BUILDKITE_MESSAGE
    - GITHUB_RELEASE_ACCESS_TOKEN
    - SSH_AUTH_SOCK=/ssh-agent
